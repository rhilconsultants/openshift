apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: gogs-ingress
  namespace: gogs
spec:
  rules:
  - host: gogs.example.com
    http:
      paths:
      - path: /
        backend:
          serviceName: app-gogs
          servicePort: 3000